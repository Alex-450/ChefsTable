<div class='container padding-200 '>
  <%= simple_form_for [@booking, @review] do |f| %>

    <div class="form-inputs">
      <%= f.input :comment, placeholder: "Type your review..." %>
      <%= f.input :rating %>
    </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
  <% end %>
<div>
